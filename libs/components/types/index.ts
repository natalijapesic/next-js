export enum ButtonStyle {
  dark = 'dark',
  light = 'light',
  disable = 'disable',
  prev = 'prev',
  next = 'next',
  redRound = 'redRound',
  search = 'search',
}

export enum InputStyle {
  rounded = 'rounded',
  bottom = 'bottom',
}
